<footer class="footer">
    <div id="wxqr" class="modal">
        <div class="modal-background"></div>
        <div class="card is-flex" style="flex-flow:column;width:300px;align-items: center;padding: 10px;">
            <p>博客的内容也<strong>将</strong>会在公众号上同步更新</p>
            <img src="https://storage.idx0.dev/Images/qrcode_for_gh_3274497ccd21_258.jpg"
                style="width:250px;height:250px" alt="">
            <p class="is-hidden-tablet">按住👆的二维码识别</p>
            <p class="is-hidden-mobile"> 打开微信 扫一扫</p>
        </div>
        <button class="modal-close is-large" aria-label="close" onclick="$('#wxqr').removeClass('is-active')"></button>
    </div>
    <div class="container">
        <div class="level">
            <div class="level-start has-text-centered-mobile">
                <a class="footer-logo is-block has-mb-6" href="<%- url_for('/') %>">
                <% if (logo && logo.text) { %>
                    <%= logo.text %>
                <% } else { %>
                    <img src="<%- url_for(logo) %>" alt="<%= title %>" height="28">
                <% } %>
                </a>
                <p class="is-size-7">
                &copy; <%= date(new Date(), 'YYYY') %> <%= author || title %>&nbsp;
                Powered by <a href="https://hexo.io/" target="_blank">Hexo</a> & <a
                        href="https://github.com/ppoffice/hexo-theme-icarus" target="_blank">Icarus</a>
                <% if (busuanzi) { %>
                <br>
                <span id="busuanzi_container_site_uv">
                <%- _p('plugin.visitor', '<span id="busuanzi_value_site_uv">0</span>') %>
                </span>
                <% } %>
                </p>
            </div>
            <div class="level-end">
            <% if (Object.keys(links).length) { %>
                <div class="field has-addons is-flex-center-mobile has-mt-5-mobile is-flex-wrap is-flex-middle">
                <% for (let name in links) {
                        let link = links[name]; %>
                <p class="control">
                    <a class="button is-white <%= typeof(link) !== 'string' ? 'is-large' : '' %>" target="_blank" title="<%= name %>" href="<%= url_for(typeof(link) === 'string' ? link : link.url) %>">
                        <% if (typeof(link) === 'string') { %>
                        <%= name %>
                        <% } else { %>
                        <i class="<%= link.icon %>"></i>
                        <% } %>
                    </a>
                </p>
                <% } %>
                </div>
            <% } %>
            </div>
        </div>
    </div>
</footer>